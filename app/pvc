apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jar-pvc
  namespace: mcc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: rook-cephfs
